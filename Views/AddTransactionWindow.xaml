<Window x:Class="MyFinance.Views.AddTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить транзакцию" Height="350" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Тип:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="TypeComboBox" Grid.Row="0" Grid.Column="1" SelectedIndex="0">
            <ComboBoxItem Content="Income"/>
            <ComboBoxItem Content="Expense"/>
        </ComboBox>

        <TextBlock Text="Сумма:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="AmountTextBox" Grid.Row="1" Grid.Column="1"/>

        <TextBlock Text="Категория:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="CategoryTextBox" Grid.Row="2" Grid.Column="1"/>

        <TextBlock Text="Счёт:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="AccountTextBox" Grid.Row="3" Grid.Column="1"/>

        <TextBlock Text="Дата:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="DatePicker" Grid.Row="4" Grid.Column="1" />

        <TextBlock Text="Примечание:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Top"/>
        <TextBox x:Name="NoteTextBox" Grid.Row="5" Grid.Column="1" Height="60" AcceptsReturn="True"/>

        <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="5" Click="OkButton_Click"/>
            <Button Content="Отмена" Width="80" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>